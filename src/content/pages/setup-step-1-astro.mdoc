---
title: "setup-step-1-astro"
seoTitle: "Schritt 1: Astro & Lokale Installation"
seoDescription: "Einrichten der Entwicklungsumgebung und Starten des Astro-Projekts."
---

# 🚀 Schritt 1: Das Fundament legen

Bevor wir bauen können, brauchen wir werkzeug und einen leeren Bauplatz. Wir nutzen **Astro**, ein Framework, das für **Content-Driven Websites** (wie Portfolios, Blogs, Firmenwebseiten) perfektioniert wurde.

## 📋 Voraussetzungen

Du brauchst auf deinem Computer:
*   [Node.js](https://nodejs.org/) (Version 18 oder neuer).
*   Einen Code Editor (Wir empfehlen [VS Code](https://code.visualstudio.com/)).
*   Ein Terminal (Eingabeaufforderung oder iTerm).

## 1. Projekt erstellen

Öffne dein Terminal und führe diesen Befehl aus. Er erstellt einen neuen Ordner und lädt alles herunter, was du brauchst:

```bash
npm create astro@latest mein-neues-projekt
```

Der Installations-Assistent fragt dich ein paar Dinge:
*   **Template?** Wähle `Empty` oder `Minimal` (wir wollen sauber starten).
*   **TypeScript?** Wähle `Strict` (hilft Fehler zu vermeiden).
*   **Dependencies installieren?** `Ja`.
*   **Git initialisieren?** `Ja`.

## 2. Bibliotheken installieren

Wir brauchen ein paar Helferlein für unser Design und das CMS. Gehe in deinen neuen Ordner:

```bash
cd mein-neues-projekt
```

Installiere **Tailwind CSS** (für das Styling) und **React** (für das CMS Dashboard):

```bash
npx astro add tailwind react netlify
```
Drücke einfach immer `Enter` (Ja), wenn er fragt.

## 3. Starten!

Jetzt schauen wir uns das Ergebnis an:

```bash
npm run dev
```

Öffne deinen Browser und gehe auf `http://localhost:4321`. Du solltest eine leere, weiße Seite sehen (oder ein Astro Logo).

**Glückwunsch!** Dein Server läuft.

---

### Was ist gerade passiert?
*   Du hast einen lokalen Webserver gestartet.
*   **Astro** überwacht jetzt deine Dateien. Wenn du etwas änderst, aktualisiert sich der Browser automatisch.

---
👉 [Weiter zu Schritt 2: Das CMS einrichten](/setup-step-2-keystatic)
